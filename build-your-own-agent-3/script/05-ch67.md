# CH6 规则与记忆 + CH7 工程文化与收口（P26–P32）逐页讲解脚本

---

## P26 · 第 06 章 · 规则与记忆

**一句话主旨**：这一章只回答一个问题——「让 agent 别犯错」这件事，到底该写在哪一层。

**提词**：
- 四十秒过，扉页不承载信息
- 章问题：写在哪一层 [重]
- 两页：三桶路由 / 代价页
- 进度点第六个亮，还剩一章
- 底下紫框那句是全章的钥匙 [慢]
- 不是「TTSR 取代 prompt 规则」[停]

**怎么讲**：
上一章我们停在那张最诚实的 benchmark 上——小 payload 上 Rust 反而慢 0.86 倍，作者自己在注释里写「若更慢，不要切换调用点」。那是「性能这件事怎么判断该不该做」。这一章换一个题目，但你会发现它的思路是一模一样的。（停一拍）看左边这个 06，标题叫「规则与记忆」。加粗那行是本章唯一的问题：**「让 agent 别犯错」，到底该写在哪一层？** 我先把大家脑子里最常见的答案说出来——写在 system prompt 里。「你不许用 any」「你不许删库」「你改完要跑测试」，一条一条堆进去。这个答案不能说错，但它只是一层。omp 这个仓库里同时存在三套互不相干、耦合度极低的机制在干这件事，而且——这句话请记住——**它们的默认开关状态本身就是作者的价值排序**。TTSR 默认开，advisor 默认关，记忆默认关。往下这行小字是本章的收束：同一份规则文件，按错误的时间尺度分流到不同的执行时机。右边小目录两页：27 页讲三桶路由，28 页是代价页——那一页的副标题我先念一下，「309 次 Stop.」，这是仓库里一个 issue 的实测数字，等会儿会展开。进度点第六个亮了，我们还剩一章。最后是底下这个紫框，本章的钥匙就在这儿：**不是「TTSR 取代 prompt 规则」，而是秒级的错误交给流式匹配，恒定的约束交给 prompt。** 这句话我等一下在 27 页会再说一遍，因为整章就靠它立着。

**别漏了说**：
- 「默认开关状态本身就是价值排序」这句要说出口——它是 27、28 两页的共同读法，也是本章跟 CH5 那张 benchmark 的呼应点：作者一贯地把「不敢默认打开」当成一种诚实。
- 章问题要念成「写在哪一层」，不要念成「怎么让 agent 别犯错」。差别在于：后者是求解，前者是分层——本章不提供更好的规则，只提供规则的放置方式。
- 时间控制：这页四十秒，别在扉页展开三套机制的细节，27、28 页都要用。

**可能被问**：
- Q：三套机制指哪三套？扉页上没写全。
  A：TTSR（流式规则）、advisor / watchdog（第二个模型做二次评审）、mnemopi（跨 session 的本地记忆）。27 页主讲 TTSR 的分桶，28 页主讲 advisor 的代价，记忆只在 28 页右栏点四条天花板。完整的三套对照在仓库 research/R06 的第 0 节。
- Q：记忆为什么不单独给一页？
  A：因为它默认是关的，而且我读下来它最有价值的部分是「它做不到什么」——没有向量索引、O(N) 全表扫、10000 行硬上限、事实去重是精确字符串匹配。这些放在 28 页右栏当「天花板」讲，比单独吹一页表结构诚实。

**过渡到下一页**：那我们就把这三个桶摊开——下一页是这一章、也可能是全场最该被抄走的一张设计图。

---

## P27 · 同一份规则 markdown，按 frontmatter 分成三个桶

**一句话主旨**：同一份规则文件按 frontmatter 自动路由到三种执行时机，prompt 占用分别是 0、全文、一行。

**提词**：
- 最该被抄走的一页，讲慢一点 [慢]
- 输入只有一种：带 YAML 头的 markdown
- 桶 1 TTSR：有 condition → 占用 0 [重]
- 桶 2 alwaysApply → 全文，固定税
- 桶 3 只有 description → 一行索引，rule:// 按需读
- 判据优先级：TTSR 最高
- 右栏五步：匹配 → abort → 删半截 → 注入 → 重跑
- 红利：规则可测试、可生成
- 代价：O(n²)、重跑丢 token
- 收口那句要念全 [停]

**怎么讲**：
这一页我讲得最慢，因为如果今天全场你只带走一张图，我希望是这张。（停一拍）先看最上面那行小字：**同一份 markdown，YAML frontmatter + 正文**。请注意「同一份」三个字——这套系统的输入只有一种东西，就是一个带 YAML 头的 markdown 文件，正文是给模型看的整改说明，带 Bad / Good 对照。这些文件从哪来？第二行：按 provider 优先级，native 100、omp-plugins 90、agents 70、cursor 50、windsurf 50、cline 40，按规则名去重，first-wins。也就是说你项目里的 `.cursor/rules`、`.clinerules`、`RULES.md` 它全收，同名的按优先级挑一个。第三行是仓库自带的：**27 条内置规则**，ts-no-any、ts-import-type、rs-parking-lot、go-range-int 这些，一眼看得出是按语言分的 lint 类约束。

（停一拍）关键来了：**这一份文件进来之后，走哪条路，不是人选的，是看 frontmatter 里有什么字段自动分的。** 三个桶。

第一个紫框，桶 1，TTSR。判据写在这儿：**有 `condition` 或 `astCondition`，优先级最高**。举两条真实的：`ts-no-any` 的 condition 就是一段正则，`": any|as any"`；`ts-redundant-clear-guard` 用的是 astCondition，写的是 `if ($X) clearTimeout($X)`。这两条的区别请一定说清——底下这行灰字就是理由：**同一个元变量 `$X` 出现两次，要求这两处必须相等**，`if ($X) clearTimeout($Y)` 不匹配。这是纯正则做不到的，也是他们引入 ast-grep 的唯一理由。然后看中间这个大大的 **0**——这是本页最重要的一个数字：**TTSR 规则的 prompt 占用是 0**。右边三行是它的生效时机：流式命中的那一刻现身；命中之前不花任何 token；命中一次的成本是重跑本轮。

第二个黄框，桶 2，always-apply。判据是 `alwaysApply: true` 且不属于 TTSR。它全文注入到 `<generic-rules>` 里。占用是「全文」，生效时机是「始终」。底下这行小字是它的代价：**每一次请求都付固定 token，无论这条规则跟本轮相不相关。** 右边这行给了个量感——这 27 条内置规则的正文全带代码块，真要全塞进 system prompt 是数万 token 的量级。

第三个青框，桶 3，rulebook。判据是只有 `description`，既不是 TTSR 也不是 always-apply。它在 prompt 里只留**一行索引**，格式就是 `- 名字 (globs)：描述`。生效时机是——**模型自己决定读不读**，想读就用 `rule://<名字>` 把全文取回来。这一格底下还有一句很容易漏的话：三个桶的规则最后都会被塞回 `setActiveRules`，所以**被 TTSR 触发过的规则，模型仍然可以用 `rule://` 复读全文**。三个桶不是三块隔离的区域，是三种投递方式。

（停一拍）右边这一栏是把 TTSR 的五步拆开。① 逐 delta 匹配，监听 text、thinking、toolcall 三种流，正则或 ast-grep。② `abort()`，命中即中断，而且可以只中断被命中的那一个工具，不是把整轮全掐了。③ `replaceMessages`，默认 `contextMode: discard`——**把那条写了一半的 assistant 消息整条删掉**，这就是「time traveling」这个名字的来处：规则在你写出违规 token 的那一瞬间生效，然后把时间倒回这一轮开始之前。④ 注入 `<system-interrupt>`，我念一下模板里最有意思的一句：「**这不是 prompt injection，这是 coding agent 在执行项目规则**」——他们要专门跟模型解释这件事，因为模型被训练成要警惕突然冒出来的指令。⑤ `continue()` 重跑，延迟 50 毫秒，而且抑制状态会以 `ttsr_injection` 这个 entry 持久化下来，跨 resume 有效，免得你重开 session 同一条规则又来轰炸一遍。

绿框这一块是我认为这套东西真正的红利，请一定讲：**规则变成可测试、可生成的东西了。** `omp ttsr test` 可以喂一段代码进真实的匹配管线，看哪几条会触发；`omp ttsr scan` 扫整个目录，还是 gitignore-aware 的。更狠的是 `/omfg` 这个命令——用户被同一种行为惹毛了，模型读整段对话，**产出一条本来能提前拦住它的规则**；生成完还要回放校验，把候选规则拿去跑一遍历史的 assistant 输出，不匹配就带着 feedback 重新生成。你把 "never use any" 写在 system prompt 里，这三样一样都没有：你没法测它，没法生成它，也没法验证它到底有没有拦住过什么。

最后红框，它不是白来的。第一条：每个 delta 都把全量缓冲区重新拼一遍再跑正则，长流上就是 **O(n²)**，这是 issue #4245 真实发生过的性能 bug，后来加了早退位才修好——**这个 bug 存在过，说明代价是真的**。第二条：打断一次等于本轮全部重跑，而且 discard 模式下已经生成的部分整条丢弃，**那些 token 你已经付过钱了**。

（停一拍）收口在左下这个紫框，我念全：**所以准确的说法不是「TTSR 取代 prompt 规则」，而是同一份规则文件，按时间尺度配不同的执行时机——秒级的错误交给流式匹配，恒定的约束交给 prompt，其余的留给模型按需取。** 底下这行是给工程师的换算：**TTSR = prompt 预算为 0 的规则；always-apply = 每次请求都要付的固定税。** 这两个词你带回去就能直接用在自己的系统上。

**别漏了说**：
- 「同一份文件、自动分流」这个机制本身比三个桶的具体内容更值钱。很多人的第一反应是「我也搞个流式拦截」，那是抄功能；真正该抄的是「规则不该只有一种执行时机」。
- 那个 **0** 要指着念出来，并且立刻接一句「代价在命中时才付」——否则听众会以为是免费午餐。
- 27 条这个数字我逐文件核过：目录下 28 个条目，其中一个是 `index.ts`，`.md` 规则是 27 条。仓库研究笔记 R06 里写的是 28 条，那是把 `index.ts` 一起数了。以页面上的 27 为准。
- ast-grep 那条一定要讲元变量两处相等，这是「为什么不能只用正则」唯一站得住的技术理由，少了它 ast-grep 就显得是炫技。
- 「TTSR 命中过的规则仍可 `rule://` 复读」这句别跳——它说明三个桶是同一个规则池的三种投递方式，不是三份拷贝。

**可能被问**：
- Q：正则会不会误杀？`": any|as any"` 在注释里、字符串里、markdown 里都会命中吧。
  A：会，而且这是设计上认了的。他们的缓解手段不是把正则写得更精确，而是用 `scope` 收窄到流上——`ts-no-any` 的 scope 是 `tool:edit(*.ts)` 这一类，也就是只在编辑 TS 文件的那股流上匹配，不管你在正文里写了什么。这是个很值得学的取舍：**降低误杀靠缩小作用域，不靠提高模式精度。**
- Q：这跟 lint / pre-commit hook 有什么区别？我 CI 上跑 eslint 不就行了？
  A：区别在时机。lint 是在模型把整个文件写完、工具执行完、你 review 的时候才发现；TTSR 是在那几个 token 刚吐出来的时候就掐掉，然后让模型带着规则重写一遍。省的是「写完—发现—再让它改一遍」这一整个来回。但反过来说，如果你的错误容忍到 CI 才发现也不亏，那你确实不需要 TTSR，用 lint 更便宜——这也是本页要传达的判断方式。
- Q：桶 3 的 `rule://` 靠模型自觉去读，它会读吗？
  A：不保证。这正是三个桶的差别所在：桶 1 是确定性执行、不依赖自觉；桶 2 是常驻但要付固定税；桶 3 最便宜但最不可靠。所以规则该进哪个桶，本质上就是在问「这条规则漏了会死人吗」。我没有在仓库里找到「模型实际读取 `rule://` 的比例」这类数据，这个我不知道。
- Q：`interruptMode` 有几档？默认是什么？
  A：全局默认是 `always`，也就是命中就中断重跑，规则级可以覆盖成 `never`。但下一页你会看到一个很反差的事实——先卖个关子。
- Q：这套东西对延迟的影响有多大？
  A：仓库里有 O(n²) 的修复记录，但**没有 benchmark 数据**，「一次匹配是微秒还是毫秒级」我答不上来，这是我这轮取证的空白。

**过渡到下一页**：讲到这儿这套东西听起来太美好了，所以下一页我专门做了一张代价页——而且这一页最有说服力的证据是：**作者自己也不敢默认打开它。**

---

## P28 · 作者自己也不敢默认打断

**一句话主旨**：三条代价都是仓库里能查到的原话和数字，其中最有力的一条是 27 条内置规则全部选了「不打断」。

**提词**：
- 三条代价，全是原话和数字，不是推测 [重]
- ① 27 条内置规则，27 条全是 never [慢]
- 能力做出来了，默认档挑最保守的
- ② 309 / 92 / 114 / 52 / 41
- "make the rules load-bearing in code instead of prose"
- 抑制必须对模型不可见 —— 否则 Stop. 换成 Halt. [重]
- ③ advisor 默认走强模型，省钱不靠降档
- 五道闸：默认关 / 只喂增量 / 四层闸门 / 冷却 3 轮 / 背压 30s
- "silence is the correct expression of 'no concerns'"
- 右栏：换到了 prompt-only 给不了的三样
- 记忆四条天花板
- 收口：搬走的是「时间尺度 → 执行边界」[停]

**怎么讲**：
上一页我把 TTSR 讲得很漂亮，这一页专门泼冷水。副标题我先声明一下：**这三条代价，全部是仓库里能查到的原话和数字，不是我的推测。** 有几条我甚至是逐个文件数过的。（停一拍）

第一条，也是我个人认为整场最有说服力的一条证据。全局默认的 `interruptMode` 明明是 `"always"`——能力是做出来的，命中就 abort、就丢弃、就重跑。但是：**仓库自带的 27 条内置 TTSR 规则，27 条，一条不落，全部显式写了 `interruptMode: never`。** 这个我是逐文件 grep 过的，不是抽样。never 意味着什么？意味着不 abort，只是把提醒渲染成一个 text block，**前置到工具结果的第一个 block 之前**——就是这行代码：`return { content: [{ type: "text", text: reminder }, ...ctx.result.content] }`。模型看到工具结果的时候，先看到那句提醒。（停一拍）所以这一页的读法写在最下面这行红字：**打断的能力做出来了，但默认档位挑了最保守的那一档。** 一个人把这套流式打断系统从头写出来，最后自己给它配的默认值是「不要打断」。这比我说十句「谨慎使用」都有力。

第二条，advisor。advisor 是可选的第二个模型，每个主 turn 结束后看主 agent 的增量转录，通过 `advise` 把意见反向注入回来。听起来很合理对吧？现在看这排数字，这是 issue #3520 记录的**一个 session**：**309 次 advise 调用，92 条唯一内容，其中 114 次是 "Stop."，52 次是「无问题，继续」，41 次是「完成」。** 309 次里只有 92 条是不重复的，剩下三分之二是复读。而且——念这行——**任务早就做完了，主转录还在被 `<advisory severity="blocker">Stop.</advisory>` 一条条灌满。**（停一拍）他们的修复注释我原话念一遍：**"make the rules load-bearing in code instead of prose."** —— 让规则在代码里承重，而不是待在散文里。这句话我认为是整个仓库里最值钱的一句话，因为它可以直接反过来用在你的项目上：你 prompt 里那些「请不要反复提醒用户」「请保持简洁」，全是 prose，全都不承重。

然后是这一页第二有意思的细节，请一定讲：**抑制必须对模型不可见。** 被丢弃的 advise 调用，工具照样返回 `"Recorded."`，就跟成功了一样。为什么？理由白纸黑字写在注释里：如果你告诉它「你这条被拦下来了」，它会把 `"Stop."` 换成 `"Halt."`，再换成 `"Stop now."` 绕过去。（停一拍）我第一次读到这儿是有点毛骨悚然的——这不是模型在恶意规避，它只是在尽职地完成「我要提醒主 agent 停下」这个目标，反馈信号让它学会了换个说法。**你给模型的每一个失败反馈，都可能被它当成一次优化机会。** 这条经验适用于任何你打算给 agent 加限流、加配额、加过滤的场景。

第三条讲钱。直觉上第二个模型应该用小模型对吧？错。`ROLE_PRIORITY_ALIAS` 里写着 `advisor: "slow"`——**advisor 默认解析到 slow 强推理模型链，而且不继承主模型**，所以哪怕你主 agent 用的是便宜模型，advisor 还是一个独立的强模型。省钱**不靠降档**。靠什么？靠另外五道：第一，**默认关闭**——`advisor.enabled` 默认 false。第二，**只喂增量转录**，每轮只发 delta，不发全量。第三，**四层输出闸门**，底下这行写全了：归一化（`"Stop."`、`"*Stop*"`、前后带空格的 `stop` 全归一到一个 `stop`）→ 无内容短语黑名单 → 全 session 精确去重，4096 条 FIFO → 每个 advisor 周期最多放行一条。第四，**打断冷却**：`immuneTurns` 默认 3，一次成功打断之后，接下来 3 个主 turn 里的 concern 和 blocker 全部降级成不打断。第五，**背压不是锁步**：主 agent 在 turn 结束后最多等 30 秒让 advisor 追上，等不到就走——这 30 秒也正是它默认关掉的原因之一。最后这行注释原话也很好：**"silence is the correct expression of 'no concerns'"**——没问题的正确表达方式是沉默。这一句可以直接抄进你自己给 review agent 写的 prompt 里。

（停一拍）右边这一栏是平衡。绿框：它换到了 prompt-only 给不了的东西——可测试、可生成、可回放校验，这三样上一页讲过了，我这里只重复一句：**system prompt 里的一句 "never use any" 没有这三样。** 下面紫色这四块是顺带把记忆的天花板说清楚，因为它也是默认关的，我不打算给它单开一页，但这四条你得知道：一，向量是以 JSON 文本存的，`memory_embeddings.embedding_json`，而且 sqlite-vec 的路径写了但**从来不建表**。二，所以召回是**暴力全表扫，10000 行封顶**，规模上会先撞墙。三，事实去重是 SPO 精确匹配——主谓宾三个字段一模一样才算重复，所以**换个说法的同一件事会被记两遍**，这是记忆膨胀的现实入口。四，工作记忆 TTL 24 小时，但巩固窗口只有一半、12 小时；**没赶上巩固的，24 小时后是真 DELETE，不是软删。**

最后这个框是本章的收口，我念：**这一章能搬走的，不是那 27 条规则，也不是那套记忆表结构；是「按错误的时间尺度，给它配一个执行边界」——秒级用流式匹配，轮级用第二意见，跨 session 用记忆。以及：把 prose 里的软约束，落成代码里的硬闸门。**

**别漏了说**：
- 「27 条全是 never」是本页的王牌，必须说清是**逐文件核过的、不是抽样**，因为它承载的是一个很强的推论（作者自己不敢默认打断）。
- "make the rules load-bearing in code instead of prose" 建议英文原话念一遍再翻译，这是全场最值得被抄进笔记的一句。
- 「抑制对模型不可见」这条要展开讲后果，别只念结论。它是本页唯一一条能直接迁移到听众自己系统上的反直觉设计。
- advisor **不降档**这件事要点破，因为所有人的直觉都是「监督者用小模型」。它选择了「贵但默认关」而不是「便宜但默认开」——这又是一次价值排序。
- 记忆那四条讲快一点，它们是补充不是主线；但「真 DELETE」和「换个说法记两遍」两条别省，这是听众自己做记忆系统时最容易踩的两个坑。

**可能被问**：
- Q：309 次是不是极端个例？平均是多少？
  A：不知道。仓库里只有这一个被 issue 记录下来的 session 快照，没有分布数据。我讲它是因为它触发了一次真实的代码修复，不是因为它有代表性。
- Q：那 advisor 到底值不值得开？
  A：仓库没给结论，我也不替大家下。我能给的判断依据是：它补的是 prompt 和正则都覆盖不了的那一层——设计判断、方向漂移、过早宣布完成。这类问题写不成正则，也很难靠同一个模型自查，因为它已经说服自己了。如果你的场景里这类错误代价很高（比如无人值守长跑），那双倍成本可能划算；如果你全程盯着，那你本人就是 advisor。
- Q：TTSR 默认开、advisor 默认关，是不是因为 advisor 还不成熟？
  A：我倾向于不是成熟度问题，是成本形态不同。TTSR 不命中就零成本，默认开是安全的；advisor 是每轮固定一次强模型调用，默认开等于所有人的账单翻倍。这是我的读法，不是仓库原话。
- Q：advisor 的工具集有安全问题吗？
  A：有一条文档里明确标注的信任边界：`WATCHDOG.yml` 可以把 advisor 的工具池扩到任意内置工具，包括 `edit`、`bash`、`eval`，而且**advisor 的工具不走主 agent 的审批包装**——授予即可直接调用。默认工具池是只读的 read / grep / glob 加一个 advise，但这个口子是敞着的。
- Q：记忆那套东西 O(N) 全表扫，10000 行大概是多久的量？
  A：这个我没实测，也没在仓库里找到换算。只能说结构上它就是个会先撞墙的实现。

**过渡到下一页**：前六章我们一直在讲「它做了什么」。最后一章换个角度——讲它**是怎么判断该不该做的**，以及这 153 万行里你真正能搬走的是哪几条。

---

## P29 · 第 07 章 · 工程文化与收口

**一句话主旨**：最后一章不讲功能，只讲判断方式，以及一条贯穿三页的主线——规范和执法者必须成对出现。

**提词**：
- 视角切换：从「做了什么」到「怎么判断该不该做」[重]
- 三页：文档编译进二进制 / 靠量化 / 收口清单
- 三条其实是同一件事 [慢]
- 规范 + 执法者成对出现，规范才不会腐烂 [重]
- 进度点满了，最后一章
- 本章只回答：你能搬走哪几条 [停]

**怎么讲**：
七个进度点，全亮了，这是最后一章。（停一拍）先说这一章跟前面六章的关系，这句话写在标题下面：**前六章讲的是它做了什么，这一章只讲一件事——它是怎么判断该不该做的。** 为什么把这个放最后？因为前六章你听下来，很可能会有一种「哇好厉害但跟我没关系」的感觉——一个人写了 153 万行，8.4 万行 Rust，自建 CI 集群，你抄不动，也不该抄。但**判断方式是可以抄的，而且是免费的**。

往下这行小字列了本章三页的内容，我先说一件事：这三条乍看毫不相干——文档编译进二进制给 agent 自己读、每条禁令后面挂 issue 编号、edit 格式好不好靠基准量出来。但它们是同一件事的三个面，那句共同点写在这行的最后：**规范和执法者成对出现，规范才不会腐烂。** 文档的执法者是 agent 自己——它读到过时的文档就会犯错，错误立刻暴露文档没更新；禁令的执法者是 issue 号和 smoke test；「什么样的 edit 格式好」的执法者是那个 benchmark。（停一拍）反过来说，你项目里那些只有规范没有执法者的东西——比如一份没人跑的 CONTRIBUTING、一条写在 wiki 里的命名约定——它们不是没用，它们是**正在腐烂**。

右边小目录三页：30 页讲文档编译进二进制，122 篇、27,931 行打成两行注入；31 页讲量化，自建 edit 基准跑 6 个模型，成绩签进仓库；32 页是收口，能抄的五条、别抄的六条。最后这个绿框是本章唯一的承诺：**这 153 万行里，你真正能搬走的是哪几条。**

**别漏了说**：
- 「判断方式可以抄，而且免费」这句要点破，它是听众坚持听到最后一页的理由。
- 「三条其实是同一件事」要在这里就埋下，30、31 两页都会回扣到「规范 + 执法者」这一句上，32 页则是它的总账。
- 进度点满格这个视觉要提一句，跟第一章的约定收尾。

**可能被问**：
- Q：工程文化这种东西能算技术分享吗？
  A：我理解这个疑问。我的选择是：只讲**有代码坐标、有数字、有原话**的部分——每条禁令挂的 issue 号是真的，122 篇文档的编译脚本是真的，6 个模型的成绩是签在仓库里的 JSON。不讲「团队氛围」「工程师精神」这类无法核查的东西。
- Q：这一章会讲 robomp 那个自托管机器人吗？
  A：不单独讲，它出现在 32 页的「别抄」里会一笔带过。它的安全设计很值得读——双容器信任边界、HMAC sidecar、凭据脱敏审计、崩溃后续接会话——但那是「agent 自动修自己仓库的 issue」这个具体目标才需要的规模，跟今天的主线关系不大。完整的八条在 research/R09 第 4.2 节。

**过渡到下一页**：先从最反直觉的一条开始——他们把 122 篇文档编译进了二进制，而这些文档的读者不是人。

---

## P30 · 122 篇文档被编译进二进制，读者是 agent 自己

**一句话主旨**：文档在编译期打包进二进制、运行时由 agent 通过 `omp://` 自己懒解压查阅，目的是让检索路径和阅读路径重合。

**提词**：
- 一份来源，两个消费者 [重]
- docs/ 122 篇 27,931 行 → compile-binary.ts → PI_DOCS_EMBED
- 注入的就两行：文件名数组 + 一大坨 base64
- 列目录只解析第 1 行，一个正文都不解压
- 真读某篇才在 zlib 线程池上懒解压一次
- dev 树绕开嵌入，直接读磁盘 —— 永不分叉
- 原话：过时的速度比任何人重读它的速度都快 [慢]
- 你的最小形态：不需要编译期注入
- 右栏 ①：禁令后面挂 issue 号
- 右栏 ②：Never source-grep
- 共同点：规范和执法者成对出现 [停]

**怎么讲**：
标题里最重要的是后半句——**读者是 agent 自己**。（停一拍）左上角这行是这一页的骨架：**一份来源，两个消费者。** 顺着这条线看：最左边 `docs/` 目录，122 篇 markdown，27,931 行；中间 `compile-binary.ts` 第 42 行，gzip 加 base64；右边注入成一个叫 `PI_DOCS_EMBED` 的常量，进编译后的二进制。

底下这个框把注入的东西摊开了，其实就两行。第 1 行是文件名的 JSON 数组，`["docs/rpc.md","docs/models.md",…]`，**明文，不压缩**。第 2 行是所有正文 gzip 之后 base64，就是这一坨 `H4sIAAAA…`。为什么要拆成两行？看绿框——因为这两行对应两种截然不同的访问模式。**列目录的时候，只解析第 1 行，一个正文都不解压**；只有 agent 真的要读某一篇，才在首次读取时到 zlib 线程池上懒解压一次，之后走缓存。这是很讲究的一个设计：目录是高频操作，正文是低频操作，把高频的那部分留成明文。第三行还有一个细节别漏：**dev 树、源码 checkout 的时候绕开嵌入，直接读磁盘上的 `docs/`**。所以开发者改了 md 立刻生效，不需要重新编译；同一份 md，人读磁盘，agent 读嵌入，**永不分叉**。

（停一拍）现在讲为什么。这一段我建议大家把原话记下来，写在 `DEVELOPMENT.md` 第 7 到 10 行：**「曾经住在这里的长篇架构走查，过时的速度比任何人重读它的速度都快。」** 所以他们做了什么？把这个文件里的架构描述**删了**，改成链接过去，而不是留一份会腐烂的散文副本。（停一拍）这句话请对照一下你自己的 README——你那份「架构概览」上次更新是什么时候？

最下面这个青框是真正的收益，也是给大家的可执行版本。收益是这一句：**检索路径和阅读路径重合，所以文档不更新，会立刻被 agent 的错误暴露出来。** 这就是我上一页说的「规范和执法者成对出现」——agent 就是文档的执法者，你的文档写错了，它照着做就会犯错，你当场就知道。而且请注意最后这行：**你不需要抄编译期注入这一步。** 最小形态是：把 `docs/` 定成唯一权威，`AGENTS.md` 或 `CLAUDE.md` 里不写架构描述、只放一张「子系统 → 文档路径」的索引表。成本是一次目录整理加一条 review 规矩。

（停一拍）右边这一栏是同源的另外两条。第一条，**`AGENTS.md` 282 行，是一份执行契约，不是风格指南**。全篇 NEVER / MUST，而且每条禁令必须配一个替代物——「不要用 `ReturnType<>`」后面必须跟「用真实类型名」。为什么？因为**禁令没有替代物，agent 会自己发明一个**。但最独特的一招是这个：**禁令后面挂着它诞生的 issue 编号**。看这个例子：worker 入口用 `with { type: "file" }` → 编译二进制里 worker 静默崩溃，issues #1011、#1027；后来换成字面路径的方案 → 又要求两个构建脚本保持同步，issue #1150。（停一拍）这个 issue 号同时服务两边：**人类 reviewer 知道这条不能删**——它不是某人的洁癖，是两次线上事故换来的；**agent 遇到 edge case 时知道边界在哪**。而且它还配了活体执法者：`omp --smoke-test` 会真的 spawn 一个 worker、ping 它、退出，这个探针接进了 `ci:test:smoke`。

第二条，测试禁令：**Never source-grep**。禁止写那种读实现文件源码、然后断言它文本内容的测试——`expect(src).toContain("someCall()")`、`expect(src).not.toContain("oldName")`、「注释里必须写 X」。理由写得极清楚：**它测的是代码长什么样，而不是代码做什么。** 后果是双向的：无害的重构——重命名、换行、import 重排——会把它打挂；而行为真的坏掉的时候它照样通过。这是最坏的一种测试，两个方向的错误都占了。替代方案在底下：结构性不变量用类型测试或者 lint 规则去守，不要用字符串扫描。

最后绿框，三条收在一起：docs 有 `omp://` 兜底，禁令有 issue 号和 smoke-test 兜底，测试哲学有 review 契约兜底——**规范和执法者成对出现。只写规范不给执法者，规范就一定会腐烂。**

**别漏了说**：
- 「读者是 agent 自己」必须在开头就说破，否则整页会被误听成「他们把文档做成了离线包」。
- 「dev 树绕开嵌入」这个细节别省，它是「一份来源」这个说法成立的关键，否则就是两份会分叉的拷贝。
- 那句 DEVELOPMENT.md 的原话建议念两遍，一遍英文一遍中文——它是本页的情绪高点。
- 「你不需要抄编译期注入」这句必须说，否则听众会觉得这一页跟自己没关系。最小形态是这一页真正的交付物。
- issue 号那一招要讲「同时服务两边」，只讲一边（人或 agent）都会显得平淡。

**可能被问**：
- Q：122 篇文档谁维护？不会腐烂吗？
  A：结构上有兜底但没有自动化。我在仓库里**没有**找到「文档引用的文件路径失效就报错」这类检查——`.fallowrc.jsonc` 只管死代码。所以 docs 的准确性最终还是靠人工纪律加上「agent 犯错会暴露它」这个软反馈。这一点我如实说，它比页面上呈现的要脆弱一些。
- Q：编译进二进制之后，文档更新是不是要重新发版？
  A：对，嵌入的那份是跟着二进制走的。但开发链路上有 dev 树直读磁盘的旁路，所以本地开发不受影响。
- Q：这跟 RAG 有什么区别？为什么不做个向量库？
  A：它压根不做检索排序，只做「目录 + 按名取全文」，靠的是 agent 自己按文件名判断该读哪篇。好处是零依赖、确定性、结果可复现；代价是文档命名和索引表得写得好。我认为对 122 篇这个量级，这是正确的取舍。
- Q：把文档打进二进制会让体积涨多少？
  A：这个我没实测。参考量级是 27,931 行 markdown gzip 之后，相对于这个仓库本身 138 到 285 MiB 的分发体积可以忽略，但确切数字我给不了。
- Q：`AGENTS.md` 和 `CLAUDE.md` 有什么区别？
  A：格式上没有本质区别，都是仓库根目录给 agent 读的上下文文件，不同工具认不同的文件名。这一页讲的是内容该怎么写，跟叫什么名字无关。

**过渡到下一页**：文档那条是让规范有执法者。下一页更狠一点——他们把「哪种 edit 格式好」这种听起来纯属口味的问题，也变成了有执法者的问题。

---

## P31 · 「edit 格式好不好」被做成了可测量问题

**一句话主旨**：自建编辑精度基准跑 6 个模型、成绩签进仓库，让「edit 格式好不好」从口味问题变成可测量问题。

**提词**：
- 4,645 行的自建基准，成绩以 JSON 签进仓库 [重]
- 页脚已注明：仓库内快照，非本次实跑 [慢]
- haiku-4.5 拿 90，最高
- 最刺眼一行：deepseek edit_success 100%，success 55%，9 次 ghost [重]
- 落刀很准，失败在「决定该编辑什么」
- 造题：真实仓库 + AST 平凡变异
- 四档难度不改任务难度，只改要模型自己定位多少
- 判分逐字节 + formattedEquivalent 区分改错 / 缩进不对
- 右栏：CI 分片是二分实验的结论
- 10 文件 50% abort，5 文件 0/20
- 别用 bun -e 验动态库 [停]

**怎么讲**：
上一页说执法者。这一页的执法者是一个基准测试。（停一拍）标题里我用了「可测量问题」这五个字，是因为「哪种 edit 工具格式对模型更友好」——用 diff、用 str_replace、用行号、用 patch——这在绝大多数团队里是一个纯口味问题，谁嗓门大听谁的。omp 的做法是：自建一个叫 `typescript-edit-benchmark` 的包，4,645 行，跑 6 个模型，然后把成绩以 JSON **签进仓库**，换模型、换 prompt 就重跑一遍对比。

在念数字之前我先把口径说清楚，右上角这行小字：**这是仓库内快照，不是我本次实跑的。** 我本机没有 bun，跑不了。而且页脚我也标了，这份 JSON **没有标注样本量和生成时间**——从 success 都是 5 的倍数可以推测每个模型 20 个 task，但那是推测。所以下面这些数字请当成「这个团队的一次内部实验记录」来听，不要当成模型排行榜。

（停一拍）现在看表。第一行绿色的是 claude-haiku-4.5，success 90，最高。往下 kimi-k2.5 85，gemini-3-flash 80，minimax-m2.5 75，glm-4.7 65。然后是最后这行红的，我认为这一行值回整页票价：**deepseek-v3.2，success 只有 55，全场最低；但它的 edit_success 是 100.0，全场唯一的满分；同时有 9 次 ghost run，也就是空跑。**

（停一拍）这三个数字放一起是什么意思？看下面这个红框：**它每一次 edit 调用都成功了，但任务成功率最低。** 翻译成人话——**它落刀很准，但它失败在「决定该编辑什么」这一步。** 你让它改，它一改一个准；问题是它经常改错地方，或者干脆什么都不改（那 9 次 ghost run）。（停一拍）这行的最后半句是我最想让大家带走的：**这种诊断只有量化才看得见，人肉试用是看不出来的。** 你手动试几次 deepseek，感觉「诶它挺听话的呀，每次都改成功了」，你永远发现不了真正的瓶颈在定位而不是在编辑。这就是把口味问题变成测量问题的价值——它不只给你排名，它给你**归因**。

再看怎么造题，因为这决定了这份数据能说明什么。第一句：**测的是编辑精度，不是找 bug 能力。** 做法是从真实的 TypeScript 仓库取源文件，做 **AST 级的平凡变异**——注意「平凡」两个字，变异本身要简单，改个标识符、挪个语句就行，因为重点不是难度，是能不能在困难上下文里外科手术式地打上这个补丁。然后是四档难度，这一条设计得很聪明：**四档难度不改任务难度，只改「要模型自己定位多少」**。easy 直接给精确行号；medium 给所在函数；hard 是长文件加相似块，**不给位置**；nightmare 是近似重复的区域反复出现，还是不给位置。（停一拍）所以它隔离出来的变量非常干净——同样的修改内容，只改变「你得自己找多远」，测出来的就是定位能力。

判分在最下面：**逐字节比对期望产物**，同时记一个 `formattedEquivalent`——就是用 prettier 归一化之后是否等价。为什么要记这个？为了**区分「改错了」和「只是缩进不对」**。这两种失败的严重程度完全不同，混在一起统计你就什么都看不出来了。

（停一拍）右边这一栏是同一种做法用在另一个地方——CI。上面这个框是他们测试分片崩溃的排查结论，直接写在代码注释里，英文原话：**"Bisection showed no single file is at fault — the crash is cumulative heap volume."** 没有哪个文件有问题，崩溃是累积堆体积导致的。下面红框是这次二分实验的对照数据，在强制 256MB 堆的条件下：**10 个文件一个 chunk，约 50% 的运行会 abort；切成两个 5 文件的半区，各自 0 比 20，一次都不崩。** 崩的位置也定位到了：bun 1.3.14 的 GC 标记阶段，exit 133 或 134，堆积的是 native 的 ghostty-vt cell。

所以下面这个框——**chunkSize 不是拍脑袋定的**。UI / TUI 桶 chunkSize 是 5，runtime / native 桶是 10，singleton 桶**故意不切**，因为全局状态必须共进程。每一个数字背后都有一次实验。

最后绿框三条可搬走的：① **分片逻辑离开 YAML**，进一个能本地跑、自己还有单测的脚本——他们真的有一个 `ci-concurrency.test.ts` 在守着 CI 的并发组规则。② **每个魔数旁边写清它是怎么测出来的**，这一条我觉得所有人今天回去就能开始做。③ 送一个具体的坑：**验证动态库加载别用 `-e`**。`bun -e 'require("./x.node")'` 对一个损坏的 addon 也返回 exit 0，会把 dlopen 失败吞掉；写成脚本文件才会真的失败——而且他们是**用一个损坏的 `.node` fixture 实测验证过**的，不是猜的。

**别漏了说**：
- 口径声明必须在念数字**之前**说，不能等被问了才补：仓库内快照、未标样本量与生成时间、非本次实跑（本机无 bun）。
- deepseek 那一行要念全三个数字（55 / 100 / 9），少任何一个归因就不成立。
- 「四档难度只改定位距离，不改任务难度」是这份基准科学性的关键，别讲成「四档难度递增」。
- `formattedEquivalent` 这个字段要讲它解决什么问题，否则听起来只是个实现细节。
- 右栏的 0/20 这个对照数据要念出来，它是「魔数旁边写清怎么测出来的」这条建议的活样本。

**可能被问**：
- Q：这份成绩能当模型选型依据吗？
  A：不能。它没标样本量、没标生成时间、没标 fixture 版本，而且只测编辑精度这一个维度。它的价值在于**方法**，不在于排名。另外仓库 README 里还有另一张表（Grok Code Fast 1 从 6.7% 提到 68.3% 那组），跟这份 JSON 的模型集合**交集为空**——是两个不同批次的实验，我没有找到能复现 README 那组数字的脚本，所以我今天不讲它。
- Q：为什么 haiku 这种小模型能拿第一？
  A：因为测的是编辑精度不是推理能力——定位加落刀这件事，长上下文注意力和格式遵循比推理深度更重要。这是我的解释，仓库里没有作者的说明。
- Q：ghost run 具体指什么？
  A：空跑，即模型跑完一轮但没有产生有效的编辑。JSON 里是单独一列 `ghost_runs`。它跟 timeout 是分开统计的。
- Q：我要自己做一个，最小要多少工作量？
  A：仓库这套是 4,645 行，因为它要支持多类变异 × 四档难度 × 多语言。research 里给的估算是：**单语言、单一种变异的最小版本 200 行足够，一个周末**。20 个 case 就有用——关键是从**你自己的代码库**取素材，不要用合成代码。
- Q：CI 那个 256MB 堆的实验是他们特意做的吗？
  A：是。注释里明确写了这是「强制 256MB 堆」下的对照实验，二分定位加 0/20 的对照组，这是标准的实验设计，不是事后总结。

**过渡到下一页**：好，最后一页。前面七章一百多个细节，我把它压成两栏——你能抄的五条，和你千万别抄的六条。

---

## P32 · 能抄的五条 ／ 别抄的六条

**一句话主旨**：左边五条有坐标有成本可以直接开抄，右边六条注明了它在解什么问题——你没有那个问题，就别付那个代价。

**提词**：
- 全场收口，这一页讲满，别赶时间 [慢]
- 左栏五条：每条都有仓库坐标和成本估算
- ① AGENTS.md 带故障史 —— 投入产出比最高 [重]
- ② 编辑精度基准，20 个 case 就有用
- ③ 契约测试 + 禁 source-grep —— 跟 agent 无关的纯软工收益
- ④ CI 编排写成有测试的程序
- ⑤ 文档一份来源两个消费者
- 右栏六条：注明它在解什么问题
- ⑥ stats 绑定方式 —— 读码推断，未实跑，必须说明 [重]
- 上台前实证：omp stats + lsof -nP -iTCP:3847 -sTCP:LISTEN
- 最后主张要慢、要重 [停]
- 预告第四讲：动手写自己的实现

**怎么讲**：
最后一页。前面七章我给了大概一百多个细节，现在我把它们压成两栏。副标题先说清这两栏的写法差异，因为这个差异本身就是我的态度：**左边每一条都有仓库坐标和成本估算；右边每一条都注明它在解什么问题——你没有那个问题，就别付那个代价。**（停一拍）我不写「好的实践」和「坏的实践」，因为右边这六条在 omp 自己的语境里全都是对的。

先看左边五条。

**第一条，把 `AGENTS.md` 写成带故障史的执行契约，而不是风格指南。** 具体怎么写：第一段先消歧义——原文那句我很喜欢，「用户说 agent 的时候，指的是仓库里的那个 CLI 实现，不是你（助手）自己」，这一句解决的是 agent 在自指仓库里最容易犯的错：把用户的 bug 报告当成对自己行为的抱怨。然后是包结构表、默认工作区。规则一律 NEVER / MUST，**每条禁令必配替代物**。API 选择用「操作 | 用什么 | 不要用什么」的对照表——这种形式对 LLM 极其友好，它把「我该用哪个 API」从检索问题降级成了查表问题。最后是那一招：**每条非显然的规则挂 issue 号**，并且配一个活体执法者。成本写在底下：**一天写初版，之后每次事故补一行。** 我标了一句话——**投入产出比最高的一条**。如果今天你只做一件事，做这个。

**第二条，给你的 agent 建一个编辑精度基准，哪怕只有 20 个 case。** 上一页讲透了，这里只强调三个要点：从**你自己的代码库**取源文件，不要用合成代码；四档难度只控制上下文难度，不控制任务难度；结果**签进仓库**，换模型就重跑。成本一个周末，最小版本 200 行。

**第三条，用契约测试取代覆盖率测试，并明令禁止 source-grep。** 核心是这一句：**每个新测试要能一句话说出它守护的那一个外部可观察契约，说不出就别加这个测试。** 加上禁止断言源码文本，加上测试必须「全量套件安全」——不能做文件级的长期改写 env 或 platform，要用 spyOn 加 restoreAllMocks。（停一拍）这一条我特意标了：**它适用于任何项目，跟 agent 完全无关——这是纯粹的软件工程收益。** 你今天就算不做 agent，这条也该抄。

**第四条，CI 编排写成有测试的程序，魔数旁边写清它是怎么测出来的。** 上一页的 0/20 就是样板。

**第五条，让文档同时服务人和 agent——一份来源，两个消费者。** 再强调一次：**不需要抄编译期注入那一步**。建 `docs/` 定为唯一权威，`AGENTS.md` 只放索引表，每篇 doc 顶部列出对应的实现文件路径。成本是一次目录整理加一条 review 规矩。

（停一拍）右边六条，别抄的。我快一点，因为它们的逻辑是同一个。① **自建 Kata microVM + k3s 的 CI 集群**——它解的是「8 个平台交叉编译 Rust，且不能让 PR 代码碰共享缓存」。你没有 native 代码，GitHub 托管 runner 完全够。② **Bazel + zig 交叉编译 + 双 Rust nightly**——`MODULE.bazel.lock` 3.6 MB，Windows 交叉编译要拉 3 GiB 的 SDK，分发体积 138 到 285 MiB 一个平台，而核心包只有 111 KiB。单目标用 cargo 就好。③ **122 篇文档这个体量**——抄结构，不要抄篇数，否则你会造出一片无人维护的文档坟场。④ **1,930 个测试文件、53 万行测试**——这是 17 个大版本累积加多 provider 组合爆炸的结果，是**结果不是目标**。抄测试哲学，不要抄数量。⑤ **mnemopi 的向量检索实现**——上一章讲过了，无索引、O(N) 全表扫、10000 行硬上限、向量以 JSON 文本存。

（停一拍，语速放慢）第⑥条我要单独说，因为它牵涉到我今天的诚实纪律。这一条是 stats 面板的绑定方式：`Bun.serve({ port })` **没有传 hostname**，CORS 是 `"*"`，而同一份代码的注释自称 "the dashboard is a localhost tool"。如果它真的绑到了 `0.0.0.0`，那么同一个局域网里的任何人都能读到你完整的 token 用量、成本、项目路径和行为统计。（停一拍）**但是——我必须明确说明：这一条是读码推断，不是实跑验证。** 页面上我也这么标了。「`Bun.serve` 不传 hostname 就绑 `0.0.0.0`」这一步依赖的是 Bun 的默认行为，我本机没有 bun，**没有实测过**。前面几条我都能给你文件行号说「我亲眼读到」，这一条我只能说「我读到了前半截，后半截我推的」。（停一拍）如果各位要在自己团队里讲这一条，上台前请先自己验一遍，两条命令：先跑 `omp stats` 把面板起起来，再跑 `lsof -nP -iTCP:3847 -sTCP:LISTEN`，看它 LISTEN 在 `127.0.0.1` 还是 `*`。一分钟的事。我这次没做，所以我只能把它标成推断。

（停一拍，看着大家，慢）好，最后一句话，这是我这一场想留下的唯一一句：**omp 的价值不在它做了什么，而在它把每个决定的代价都摊开给你看了。**（停）它做了 153 万行，做了 8.4 万行 Rust，做了流式打断，做了第二个模型盯着第一个模型。但真正让这个仓库值得读的，是它每做一件事都在旁边留下了代价——Rust 更快，但小 payload 上 0.86 倍更慢，注释里写「若更慢，不要切换调用点」；打断能力做出来了，27 条内置规则却一条不落地选了不打断；advisor 很有用，但一个 session 刷了 309 次 Stop.，于是把规则从散文挪进代码；CI 的 chunkSize 是 5 还是 10，旁边写着 0/20 的对照实验。（停一拍）**所以——抄功能会死，抄它「怎么判断该不该做」才活。**

页脚右边我留了一行：**第四讲，我们动手写自己的实现。** 前三讲是地图、是下限、是上限，第四讲我们下场。谢谢大家。

**别漏了说**：
- 五条里第①条和第③条要明确标出它们的特殊性：①是投入产出比最高，③是跟 agent 无关的纯软工收益。这两条最可能被真的执行。
- 右栏每一条都要说出「它在解什么问题」，不能只念结论，否则整栏会被听成「omp 过度工程」——那不是这一页的意思。
- **第⑥条的局限声明是硬要求，不是可选项**：必须说出「读码推断、未实跑、本机无 bun」，并给出 `omp stats` + `lsof -nP -iTCP:3847 -sTCP:LISTEN` 这个实证方法。这一页的可信度全押在这句话上——前面所有「有坐标、有行号」的信誉，都是靠这里主动降级一条来兑现的。
- 最后那段主张要慢，要停，要用前面几章的具体例子把它撑起来（Rust 0.86× / 27 条 never / 309 次 Stop. / 0-of-20）。空喊这句话没有分量，举完四个例子再喊才有。
- edit benchmark 是仓库内快照这件事，如果 31 页已经说过，这里带一句即可，别重复讲。

**可能被问**：
- Q：五条里如果只能做一条，做哪条？
  A：第①条，`AGENTS.md`。一天出初版，之后每次事故补一行，而且它对人和 agent 同时生效。第③条其次，因为它连 agent 都不需要。
- Q：`AGENTS.md` 写多长合适？282 行是不是太长了？
  A：omp 是 282 行、2,570 词，但那是一个 153 万行的仓库。我的经验是：**长度应该由你的事故数量决定，不是由仓库大小决定**。从空文件开始，每次 agent 犯一个你不想再看到的错，补一行加一个 issue 号。自然长到该有的长度。
- Q：第⑥条这个安全问题，你有没有跟作者提过 issue？
  A：没有。我这次是单向取证，没有跟上游互动。而且在我实测确认之前，这只是一个读码推断，我不会拿它去开 issue。
- Q：你把「别抄」那六条列出来，是不是在批评这个项目？
  A：不是。这六条在 omp 自己的语境里全都是对的——它真的要给 8 个平台交叉编译 Rust，它的文档真的是 agent 的检索语料。我列出来是因为**分享最大的风险是听众照着抄**，而这六条抄过去就是纯成本。这也是我整场的方法：功能可以是对的，同时对你是错的。
- Q：第四讲什么时候？讲什么？
  A：具体时间待定。内容是动手写——前三讲一直在读别人的代码，第四讲我们从零搭一个最小 harness，把这三场里能落地的部分自己实现一遍。
- Q：三讲下来，你自己最大的收获是什么？
  A：就是最后那句话。我一开始是抱着「看看极致长什么样」去读的，读完最有用的反而不是它做了什么，是它每一处都留下了「为什么这么选、代价是多少」的痕迹——注释里的实验数据、issue 号、被主动降级的默认值。这些东西是免费的，而且今天就能开始做。
